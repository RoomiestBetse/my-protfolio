---
name: Visual Identity
description: Bold indigo→violet→fuchsia gradient brand, mega uppercase typography, free-flowing scroll
type: design
---
- Background: deep near-black navy `230 50% 5%`, subtle bg.jpeg at 20% opacity behind a dark overlay.
- Primary: indigo `244 90% 66%`. Accent: violet `280 90% 65%`.
- Signature gradient: `--gradient-brand` = indigo → violet → fuchsia. Used on `.gradient-text`, `.gradient-pill`, `.gradient-orb`, scroll progress bar.
- Typography: Space Grotesk 700, uppercase, tight tracking. `.mega-headline` = clamp(2.75rem, 9vw, 8rem). `.display-headline` = clamp(2rem, 5vw, 3.75rem).
- CTAs: `.gradient-pill` (filled brand gradient) and `.outline-pill` (border).
- Cards: `.glass-card` (rounded-3xl, blurred card/50, soft shadow).
- Free-flowing scroll via Lenis. NO snap-scroll.
- Copy tone: casual, personal, no em-dashes, use `|` if needed.
